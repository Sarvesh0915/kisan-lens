<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>KisanLens – Delete Account</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@600;800&family=Noto+Sans:wght@400;500&display=swap" rel="stylesheet" />
  <style>
    body { font-family: 'Noto Sans', sans-serif; }
    .brand { font-family: 'Baloo 2', cursive; }
    .leaf-bg {
      background-color: #0f4c2a;
      background-image:
        radial-gradient(ellipse at 20% 80%, rgba(34,197,94,0.18) 0%, transparent 60%),
        radial-gradient(ellipse at 80% 20%, rgba(250,204,21,0.12) 0%, transparent 50%);
    }
    .card-shadow { box-shadow: 0 20px 60px rgba(0,0,0,0.25); }
    input:focus { outline: none; box-shadow: 0 0 0 3px rgba(239,68,68,0.35); }
  </style>
</head>
<body class="leaf-bg min-h-screen flex items-center justify-center px-4 py-10">

  <div class="w-full max-w-sm">

    <div class="text-center mb-7">
      <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-red-100 mb-3 shadow-lg">
        <span class="text-3xl">🗑️</span>
      </div>
      <h1 class="brand text-3xl text-white font-extrabold">KisanLens</h1>
      <p class="text-green-300 text-sm">Delete your account</p>
    </div>

    <div class="bg-white rounded-3xl card-shadow px-7 py-8">

      <h2 class="brand text-2xl text-gray-800 font-bold mb-1">Delete My Account</h2>
      <p class="text-gray-500 text-sm mb-5">
        This will permanently remove your KisanLens profile and all associated
        data (name, phone number, age, gender, date of birth) from our servers.
        This action cannot be undone.
      </p>

      {% with messages = get_flashed_messages(with_categories=true) %}
        {% for category, message in messages %}
          <div class="mb-4 px-4 py-3 rounded-xl text-sm font-medium
            {% if category == 'error' %}bg-red-50 text-red-700 border border-red-200
            {% else %}bg-green-50 text-green-700 border border-green-200{% endif %}">
            {{ message }}
          </div>
        {% endfor %}
      {% endwith %}

      <form method="POST" action="/delete-account" class="space-y-4" onsubmit="return confirm('Are you sure? This will permanently delete your account and all your data. This cannot be undone.');">

        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">📱 Registered Phone Number</label>
          <input
            type="tel" name="phone" required placeholder="10-digit number"
            maxlength="10" pattern="[0-9]{10}"
            class="w-full border border-gray-200 rounded-xl px-4 py-3 text-gray-800 bg-gray-50 text-base transition"
          />
        </div>

        <button
          type="submit"
          class="w-full bg-red-600 hover:bg-red-700 active:scale-95 text-white font-bold text-lg rounded-xl py-3.5 transition-all duration-150 shadow-md mt-2"
        >
          Send OTP to Confirm Deletion
        </button>
      </form>

      <p class="text-center text-gray-500 text-xs mt-5">
        For safety, we'll verify your identity with an OTP before deleting anything.
      </p>
    </div>

    <p class="text-center text-green-300/60 text-xs mt-5">
      KisanLens · Built for Bharat's Farmers · Need help? kisanlens@gmail.com
    </p>
  </div>

</body>
</html>
